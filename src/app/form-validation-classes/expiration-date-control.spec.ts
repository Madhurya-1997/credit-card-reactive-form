import { ExpirationDateControl } from './expiration-date-control';

describe('ExpirationDateControl', () => {
  it('should create an instance', () => {
    expect(new ExpirationDateControl()).toBeTruthy();
  });
});
